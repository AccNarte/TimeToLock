# Database Configuration (PostgreSQL/NeonDB)
# Option 1: Use connection string (recommended for NeonDB)
DATABASE_URL=postgresql://neondb_owner:npg_tnCy3hEJj6XB@ep-falling-wind-agdzodzt-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Option 2: Use individual parameters (alternative)
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USER=root
# DATABASE_PASSWORD=password
# DATABASE_NAME=timelock
# DATABASE_SSL=true

# JWT Configuration
JWT_SECRET=changeme
JWT_EXPIRES=1d

# App Configuration
PORT=3011
FRONTEND_URL=http://localhost:3010

# Wallet Encryption (change this in production!)
WALLET_ENCRYPTION_SECRET=your-super-secret-key-change-in-production


